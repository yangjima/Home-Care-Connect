# Bug 修改计划（首轮）

本文档用于将 `C:\Users\nana\Desktop\bug.pdf` 中的问题转化为可执行修复任务。  
当前阶段仅输出计划，不进行代码改动。

## 1. 目标与原则

- 目标：优先解决「数据展示正确性」与「UI 一致性」，并完成权限与审核规则对齐。
- 原则：前后端规则一致、可回归验证、尽量小步提交、每项都具备明确验收标准。
- 范围：前端（Vue 3）为主，涉及规则约束时同步后端（Spring Boot）校验。

## 2. 优先级（按你的要求）

- P0：数据展示正确性（首页房源封面/价格、角色免审核规则）
- P0：UI 一致性（头部昵称展示、按钮尺寸一致、首页功能区文案一致）
- P1：后台上架审批筛选默认行为（默认 `all`）
- P1：后台导航结构调整（商品管理二级菜单）
- P2：首页常用功能跳转体验（无白屏感知）

## 3. 需求对齐结果（已确认）

1. 首页热门房源价格显示：使用**单价**。  
2. 超级管理员/店长上架免审核：适用于房源、服务、本地商城、二手；保留审核入口用于审核其他人内容。  
3. 上架审批筛选：前端默认类型为 `all`（或等价“全部”类型值）。  
4. 右上角昵称：最多展示 12 个字符。  
5. 用户名与邮箱：前后端都禁止修改（注册后固定）。  
6. 首页“常用功能”区域：仅改标题为“我的服务”，并保证跳转无白屏感知。  
7. 右上角下拉中“我的订单”相关项：不处理（已在个人中心）。  
8. 优先级取向：UI 一致性 + 数据展示正确性最高。

## 4. 任务拆分（可执行）

### T1（P0）首页热门房源封面与单价展示修复

- 目标：
  - 热门房源卡片封面显示正确；
  - 价格字段统一为单价。
- 涉及模块：
  - `frontend/src/views/home/HomePage.vue`
  - `frontend/src/components/*`（热门房源卡片组件，待定位）
  - `frontend/src/stores/property.ts` / `frontend/src/api/property.ts`（字段映射）
- 核心动作：
  - 梳理接口字段与前端映射关系；
  - 明确封面取值优先级并统一到单一函数；
  - 将价格渲染字段切换为单价并统一单位文案。
- 验收标准：
  - 首页热门房源封面与详情页首图一致（或符合约定回退规则）；
  - 首页所有热门房源价格显示为单价，单位一致、无错位。

### T2（P0）角色上架免审核规则全品类对齐

- 目标：
  - 超级管理员/店长创建上架内容默认免审核；
  - 审核页面仍可审核其他用户提交内容。
- 涉及模块：
  - 前端创建/提交逻辑：`frontend/src/views/admin/*CreatePage.vue`
  - 前端列表状态展示：`frontend/src/views/admin/*MgmtPage.vue`
  - 后端状态落库与权限：各服务的创建与提交流程（待逐服务定位）
- 核心动作：
  - 前端按角色传递/处理正确状态；
  - 后端作为最终兜底，按角色判定 `status`；
  - 审核列表接口保留非本人待审数据可见性。
- 验收标准：
  - 超级管理员/店长创建后状态直接可上架；
  - 普通角色创建后进入待审核；
  - 审核页仍能处理他人待审内容。

### T3（P1）上架审批筛选默认值为 all

- 目标：审批页初始筛选默认为“全部”。
- 涉及模块：
  - 审批管理页（预计 `frontend/src/views/admin/AdminListingReviewPage.vue`）
- 核心动作：
  - 初始化查询条件为 `all`；
  - 保证切换类型筛选与默认查询逻辑一致。
- 验收标准：
  - 首次进入审批页直接展示全部类型；
  - 切换筛选后可正确回到全部视图。

### T4（P1）后台菜单结构调整（商品管理二级菜单）

- 目标：在左侧菜单中将“商品管理”改为父级，包含两个二级项。
- 二级项文案（按当前约定）：
  - 本地商城商品管理
  - 跳蚤市场商品管理
- 涉及模块：
  - `frontend/src/components/layout/AdminLayout.vue`
  - `frontend/src/router/index.ts`
- 核心动作：
  - 调整菜单结构与路由映射；
  - 保证高亮、刷新恢复、直接访问路由一致。
- 验收标准：
  - 展开/收起正常；
  - 两个子菜单分别进入对应列表页面；
  - 面包屑/标题（如有）与菜单一致。

### T5（P0）头部昵称长度与资料编辑规则收敛

- 目标：
  - 右上角昵称最多显示 12 字符；
  - 用户名、邮箱在前后端均不可修改。
- 涉及模块：
  - 前端头部：`frontend/src/components/layout/AppHeader.vue`
  - 前端资料页：`frontend/src/views/user/ProfilePage.vue`
  - 前端鉴权用户状态：`frontend/src/stores/auth.ts`
  - 后端用户更新接口：`services/user-service/.../AuthServiceImpl.java` 及 DTO/校验层
- 核心动作：
  - 昵称显示函数统一做长度截断；
  - 资料编辑表单禁用 username/email 字段且不提交变更；
  - 后端拒绝 username/email 更新请求（防前端绕过）。
- 验收标准：
  - 昵称显示不超过 12 字符；
  - 提交资料后 username/email 均不变；
  - 非法请求尝试修改上述字段时后端返回可理解错误。

### T6（P0/P2）首页“我的服务”标题统一与跳转流畅性

- 目标：
  - 常用功能区域标题改为“我的服务”；
  - 功能跳转无白屏感知。
- 涉及模块：
  - `frontend/src/views/home/HomePage.vue`
  - 路由懒加载/过渡相关配置：`frontend/src/router/index.ts`（必要时）
- 核心动作：
  - 文案统一替换；
  - 优化跳转方式（避免全量刷新、确保路由 push 与组件加载体验平滑）。
- 验收标准：
  - 首页显示“我的服务”标题；
  - 连续点击功能入口时无明显白屏闪烁。

### T7（P0）统一按钮样式尺寸

- 目标：系统后台入口按钮与编辑资料按钮在视觉尺寸和风格上统一。
- 涉及模块：
  - `frontend/src/components/layout/AppHeader.vue`
  - `frontend/src/views/user/ProfilePage.vue`
  - 共享样式文件（如存在，待定位）
- 核心动作：
  - 提炼统一按钮样式类（尺寸/圆角/字重/间距）；
  - 两处复用同一类名或组件参数。
- 验收标准：
  - 两处按钮视觉尺寸一致；
  - 不影响深浅色/响应式布局（若存在）。

## 5. 执行顺序建议

1. T1 首页数据展示正确性（封面/单价）
2. T5 用户信息规则与昵称展示
3. T2 角色免审核规则
4. T6 首页标题与跳转体验
5. T7 按钮样式统一
6. T3 审批筛选默认 all
7. T4 后台菜单结构调整

> 说明：先做高风险数据与权限，再做 UI/交互结构调整，减少互相干扰。

## 6. 回归测试清单（每次提交后）

- 首页热门房源：封面、单价、跳转详情一致。
- 后台创建四类内容：按角色落正确状态。
- 审批页：默认 all、类型筛选切换正常。
- 头部昵称：超长截断到 12 字符。
- 资料编辑：username/email 不可改，接口绕过无效。
- 首页“我的服务”区域：标题正确，跳转无白屏感知。
- 后台入口按钮与资料页按钮：视觉一致。

## 7. 待你最终确认的 2 个决策项

1. 热门房源封面回退规则是否采用：`coverUrl -> images[0] -> 默认占位图`？  
2. 后台二级菜单对应路由是否沿用现有页面（不新增页面）？

---

确认后，我将严格按本计划逐项实施，并在每项完成后同步“变更文件 + 验收结果”。
